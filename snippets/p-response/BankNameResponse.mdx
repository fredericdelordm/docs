<ResponseField name="BankName" type="string" >    
The user’s bank, as defined by the `Bic` parameter sent in the call. The `BankName` is `null` if no `Bic` was provided by the platform.  
</ResponseField>